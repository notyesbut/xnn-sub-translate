# UI Package

This package will host reusable desktop UI components, tokens, and layout primitives once the desktop shell implementation begins.

The first release may keep this package minimal. It exists now to reserve a clean place for shared UI building blocks as the application grows.

