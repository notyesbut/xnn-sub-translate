# Shared Contracts

This package will contain the shared project schema, API DTOs, batch payload shapes, and status enums used across the desktop shell and the Python engine.

Contracts should be versioned carefully. If a change breaks compatibility between the app and the engine, the corresponding architecture and roadmap docs must also be updated.

