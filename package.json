{
  "name": "subtitle-rescue-workspace",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "docs:list": "find docs -maxdepth 2 -type f | sort",
    "engine:test": "PYTHONPATH=services/engine/src python3 -m unittest discover -s services/engine/tests",
    "engine:compile": "python3 -m compileall services/engine/src"
  }
}
